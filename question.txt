1.) Is Node.js a programming language?
No it is not a programming language.

2.) Is Express a programming language?
No it is not a programming language.

3.) What is the primary reason why Node/Express applications are so performant?
The primary reason why Node/Express applications are so performant is because Node. js is single-threaded and asynchronous: this means that all I/O activity does not interfere with other processes, and it is possible to send emails, read and write files, query databases, and so on, all at the same time. Each request to the web server does not have a different Node. js process.J

4.) Is...const el = document.getElementById('my-list');a valid JavaScript statement in a Node app?
No it is not a valid JavaScript statement in a Node app.

5.)What is a Model?
Model creates objects that follow the Schema.

6.)What is A View Engine?
The view engine is responsible for creating HTML from your views.

7.) What is A DataController?
The data controller determines the purposes for which and the means by which data is processed. 

8.) What is A ViewController?
A view controller acts as an intermediary between the views it manages and the data of your app.

9.) What is A RouteController?
A RouteController defines the location/url and links the DataController and ViewController.

10.) What is Express Middleware?
Express middleware refers to a set of functions that execute during the processing of HTTP requests received by an Express application. Middleware functions access the HTTP request and response objects. They either terminate the HTTP request or forward it for further processing to another middleware function.



